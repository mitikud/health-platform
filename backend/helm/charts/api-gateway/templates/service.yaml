apiVersion: v1
kind: Service
metadata:
  name: {{ include "api-gateway.fullname" . }}
spec:
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
  selector:
    app: {{ include "api-gateway.name" . }}
